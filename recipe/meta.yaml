{% set name = "bibtexparser" %}
{% set version = "1.4.0" %}
{% set sha256 = "ca7ce2bc34e7c48a678dd49416429bb567441f26dbb13b3609082d8cd109ace6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.org/packages/source/b/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script:
    - {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - pyparsing >=2.0.3
    - python >=3.6
    - setuptools
  run:
    - pyparsing >=2.0.3
    - python >=3.6

test:
  imports: bibtexparser

about:
  home: https://bibtexparser.readthedocs.org/
  license: LGPL-3.0-only OR BSD-3-Clause
  license_file: COPYING
  summary: A module for parsing BibTeX files.

extra:
  recipe-maintainers:
    - pkgw
